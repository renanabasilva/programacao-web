var banners = ["img/destaque-home.png", "img/destaque-home-2.png"]; var bannerAtual = 0; var timer = setInterval(trocaBanner, 4000); $('.pause').on('click', function (e) { e.preventDefault(); if ($(this).hasClass('pause')) { clearInterval(timer); $(this).toggleClass('pause play'); } else { timer = setInterval(trocaBanner, 4000); $(this).toggleClass('play pause'); } }); function trocaBanner() { bannerAtual = (bannerAtual + 1) % banners.length; $('.destaque img').attr('src', banners[bannerAtual]); }